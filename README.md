# OpenAi BigQuery Script

Clone code.

Create .env file:
```bash
touch .env
```

Add GCP/BigQuery credentials and OpenAI credentials into .env file.

Change SQL queries to match you bigQuery table.

run script:
```bash
python3 script.py
```
run test script:
```bash
python3 test.py
```
